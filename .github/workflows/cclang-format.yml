name: Verificação de Formatação C/C++

on:
  pull_request:
    branches: [ main, develop ]
    paths:
      - '**.c'
      - '**.h'
      - '**.cpp'
      - '**.hpp'

permissions:
  contents: read
  pull-requests: write

jobs:
  clang-format-check:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Instalar clang-format
      run: |
        sudo apt-get update
        sudo apt-get install -y clang-format
    
    - name: Verificar formatação e gerar sugestões
      id: format-check
      run: |
        # Encontra todos os arquivos C/C++ modificados no PR
        FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD | grep -E '\.(c|h|cpp|hpp)$' || true)
        
        if [ -z "$FILES" ]; then
          echo "✅ Nenhum arquivo C/C++ modificado"
          exit 0
        fi
        
        echo "📁 Arquivos a verificar:"
        echo "$FILES"
        echo ""
        
        # Verifica formatação e gera relatório
        NEEDS_FORMAT=0
        REPORT=""
        
        for file in $FILES; do
          if [ -f "$file" ]; then
            echo "Verificando: $file"
            
            # Cria arquivo temporário com código formatado
            clang-format --style=file "$file" > "${file}.formatted"
            
            # Compara com original
            if ! diff -q "$file" "${file}.formatted" > /dev/null 2>&1; then
              NEEDS_FORMAT=1
              echo "❌ $file precisa de formatação"
              
              # Adiciona ao relatório
              REPORT="${REPORT}### 📄 \`${file}\`"$'\n\n'
              REPORT="${REPORT}<details>"$'\n'
              REPORT="${REPORT}<summary>Ver alterações sugeridas</summary>"$'\n\n'
              REPORT="${REPORT}\`\`\`diff"$'\n'
              REPORT="${REPORT}$(diff -u "$file" "${file}.formatted" || true)"$'\n'
              REPORT="${REPORT}\`\`\`"$'\n'
              REPORT="${REPORT}</details>"$'\n\n'
            else
              echo "✅ $file está corretamente formatado"
            fi
            
            rm -f "${file}.formatted"
          fi
        done
        
        echo ""
        
        if [ $NEEDS_FORMAT -eq 1 ]; then
          echo "## ⚠️ Formatação de Código" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "O clang-format detetou problemas de formatação no teu código." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔧 Como corrigir automaticamente:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Formatar ficheiros específicos:**" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "clang-format -i caminho/do/ficheiro.c" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Formatar todos os ficheiros C/C++:**" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "find . -name '*.c' -o -name '*.h' -o -name '*.cpp' -o -name '*.hpp' | xargs clang-format -i" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📝 Sugestões de Formatação" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "$REPORT" >> $GITHUB_STEP_SUMMARY
          
          # Salva relatório para comentário
          echo "$REPORT" > format-report.txt
          
          exit 1
        else
          echo "✅ Todos os arquivos estão corretamente formatados" >> $GITHUB_STEP_SUMMARY
        fi
    
    - name: Comentar no PR
      if: failure()
      uses: marocchino/sticky-pull-request-comment@v2
      with:
        header: clang-format
        message: |
          ## ⚠️ Formatação de Código
          
          O clang-format detetou problemas de formatação no teu código.
          
          ### 🔧 Como corrigir automaticamente:
          
          **Formatar ficheiros específicos:**
          ```bash
          clang-format -i caminho/do/ficheiro.c
          ```
          
          **Formatar todos os ficheiros C/C++:**
          ```bash
          find . -name "*.c" -o -name "*.h" -o -name "*.cpp" -o -name "*.hpp" | xargs clang-format -i
          ```
          
          ---
          
          ## 📝 Sugestões de Formatação
          
          ${{ steps.format-check.outputs.report }}
          
          <details>
          <summary>💡 Ver diff completo no Job Summary</summary>
          
          Clica no link do workflow acima para ver as alterações detalhadas no "Summary" do job.
          </details>
